# 面试题生成

面试题生成基于候选人的结构化简历，而不是重新读取 PDF。

## 输入

用户选择一位候选人后，前端调用：

```text
POST /resumes/{resume_id}/interview-questions
```

后端读取该候选人的 `parsed_data`，让 LLM 生成定制化问题。

## 问题分类

系统要求面试题分为三类：

- 技术深挖
- 项目复盘
- 行为判断

前端按分类展示，方便面试官直接使用。

## 降级策略

如果 LLM 调用失败或返回格式不符合预期，后端会返回兜底题目，避免页面直接报错。
